node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# SQLite database files
prisma/*.db
prisma/*.db-journal

# Ignore uncompressed backups, keep .gz files
backups/*.sql
backups/*.db
!backups/*.sql.gz
!backups/*.db.gz
